<table border = '1'>
    <tr>
        <th>id</th>
        <th>name</th>
        <th>Editer</th>

    </tr>
    {% for i in list %}
    <tr>
        <td>{{i.id}}</td>
        <td>{{i.Name}}</td>
        <td><a class="btn btn-primary" href="{{path('classroom_mod',{'id':i.id})}}">Update</a>
        <a class="btn btn-primary" href="{{path('classroom_del',{'id':i.id})}}">Suprimmer</a></td>
    </tr>
    {% endfor %}
</table>