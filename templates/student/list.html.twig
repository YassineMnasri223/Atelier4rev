{{form(form)}}
<table border = '1'>
    <tr>
        <th>id</th>
        <th>NSC</th>
        <th>Email</th>
        <th>Moy</th>
        <th>Class</th>
        <th>Editer</th>

    </tr>
    {% for i in list %}
    <tr>
        <td>{{i.id}}</td>
        <td>{{i.NSC}}</td>
        <td>{{i.Email}}</td>
        <td>{{i.Moyenne}}</td>
        <td>{{i.classroom.Name}}</td>
        <td><a class="btn btn-primary" href="{{path('student_mod',{'id':i.id})}}">Update</a>
        <a class="btn btn-primary" href="{{path('student_del',{'id':i.id})}}">Suprimmer</a></td>
    </tr>
    {% endfor %}
</table>
<a class="btn btn-primary" href="{{path('student_add')}}">Ajouter</a>